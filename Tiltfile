# Tiltfile
k8s_yaml('kubernetes.yaml') # Your Kubernetes manifests
docker_build('spores', './spores') # Path to the Dockerfile and context for your 'spores' service
# Add similar lines for other services
